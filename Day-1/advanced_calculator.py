import functions as basci_calc

basci_calc
basci_calc.addition()  # agar sirf addition ko invoke karna hai tab


#packages/library : collection of modules
# pip is similar to docker cli and with pip u can download anything from pypi(pypi.org) use (pip install <name>) (pip list (se poore installed packages dikh jaate))
# virtual env ek logical seperation deta hai  packages ko in Python ( pip install virtualenv) phir use karo command ( python -m venv project-abc) check if the foolder created (ls -ltr)
# Aur gar ye virtual env use kana hai use command( source project-abc/bin/activate) examplle agar ye env me agar pip install jira kiye tu bas isi me banega wo to check (pip list | grep jira)
# agar ye virtual env me bahar aana hai tu just type (deactivate)